Empty folder to store results should the user chose to re-run the models.
Note that rerunning the models is quite computationally intensive and it is therefore reccomended to use a computing cluster.
